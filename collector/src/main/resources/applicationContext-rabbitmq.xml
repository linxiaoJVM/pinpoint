<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">

    <!--配置connection-factory，指定连接rabbit server参数 -->
    <rabbit:connection-factory id="rabbitConnectionFactory"
            host="${RABBIT.RESUME.HOST}" port="${RABBIT.RESUME.PORT}" username="${RABBIT.RESUME.USERNAME}"
            password="${RABBIT.RESUME.PASSWORD}" virtual-host="${RABBIT.RESUME.VHOST}" channel-cache-size="30"/>

    <!--通过指定下面的admin信息，当前producer中的exchange和queue会在rabbitmq服务器上自动生成 -->
    <rabbit:admin connection-factory="rabbitConnectionFactory" />
    <rabbit:template id="amqpTemplate" connection-factory="rabbitConnectionFactory" />

    <!--定义queue  说明：durable:是否持久化 exclusive: 仅创建者可以使用的私有队列，断开后自动删除 auto_delete: 当所有消费客户端连接断开后，是否自动删除队列-->
    <rabbit:queue name="yxp_log_qichejianli_analysis_queue" durable="true" auto-delete="false" exclusive="false" />

    <!--定义direct-exchange -->
    <rabbit:direct-exchange name="yxp_log_performance_analysis_ex" durable="true" auto-delete="false">
        <rabbit:bindings>
            <rabbit:binding queue="yxp_log_qichejianli_analysis_queue" key="yxp_log_qichejianli_analysis_rk" ></rabbit:binding>
        </rabbit:bindings>
    </rabbit:direct-exchange>
</beans>